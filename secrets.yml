host: "mongodb"
port: 27017
user: "dolmachi"
password: "12345"
name: "my_database"

kafka:
  bootstrap_servers: "kafka:9092"  # Адрес брокера Kafka (kafka — это имя сервиса в docker-compose)
  topic: "car_price_predictions"